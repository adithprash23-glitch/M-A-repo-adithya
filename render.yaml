services:
  - type: web
    name: mergeon-financial
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 stock_server.py
    envVars:
      - key: PORT
        value: 10000
      - key: ANTHROPIC_API_KEY
        sync: false
    plan: free
